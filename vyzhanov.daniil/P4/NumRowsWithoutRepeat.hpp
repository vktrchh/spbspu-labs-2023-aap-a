#ifndef NUMROWSWITHOUTREPEAT_HPP
#define NUMROWSWITHOUTREPEAT_HPP

#include <cstddef>

namespace vyzhanov
{
  const size_t NumRowsWithoutRepeat(const int * matrix, size_t rows, size_t cols);
}

#endif
