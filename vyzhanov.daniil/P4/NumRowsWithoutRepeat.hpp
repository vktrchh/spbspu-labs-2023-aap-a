#ifndef NUMROWSWITHOUTREPEAT_HPP
#define NUMROWSWITHOUTREPEAT_HPP

namespace vyzhanov
{
  size_t NumRowsWithoutRepeat(const int matrix[], size_t rows, size_t cols);
  size_t countWithoutRep_ = 0;
  size_t check_point_ = 0;
}
