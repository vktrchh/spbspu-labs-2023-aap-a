#include <iostream>
#include <iomanip>

char * transformString(const char * input)
{
  char * result = new char[128];
  for (int i = 0; i < 128; ++i)
  {
    
  }

}
int main()
{
  std::cin >> std::noskipws;

  std::cin >> std::skipws;
  std::cout << result << "\n";
  delete [] result;
}
