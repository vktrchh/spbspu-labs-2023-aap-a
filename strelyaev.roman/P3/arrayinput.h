#ifndef ARRAYINPUT_H
#define ARRAYINPUT_H
#include <cstddef>
namespace strelyaev
{
  void moveArray(const char *, const size_t, char *);
  char * inputArray(const char *, const size_t, size_t *);
}
#endif
