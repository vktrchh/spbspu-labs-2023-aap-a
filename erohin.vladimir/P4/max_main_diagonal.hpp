#ifndef MAX_MAIN_DIAGONAL_HPP
#define MAX_MAIN_DIAGONAL_HPP

#include <cstddef>
namespace erohin
{
  bool isOutSum(const long long & sum, const long long & a);
  long long maxMainDiagonal(int * matrix, const size_t & rows, const size_t & cols);
}
#endif
