#ifndef CHECK_MATCHES_HPP
#define CHECK_MATCHES_HPP

#include <cstddef>

namespace nikitov
{
  int checkMatches(const char* firstArray, const char* secondArray, size_t firstSize, size_t secondSize);
}
#endif
