#ifndef INPUT_ARRAY_HPP
#define INPUT_ARRAY_HPP

#include <fstream>
#include <cstddef>
namespace nikitov
{
  size_t inputArray(std::istream & input, int matrix[10000], size_t rows, size_t cols);
}
#endif
