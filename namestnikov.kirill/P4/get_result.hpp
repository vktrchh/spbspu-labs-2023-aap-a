#ifndef GETRESULT_H
#define GETRESULT_H

#include <cstddef>

namespace namestnikov
{
  size_t getResult(const int * matrix, const int * maxCol, const int * minRow, size_t result, size_t cols);
}

#endif
