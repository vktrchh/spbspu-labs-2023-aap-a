#ifndef INPUTARRAY_H
#define INPUTARRAY_H
#include <cstddef>
#include <iostream>
#include <fstream>
namespace namestnikov
{
  size_t inputArray(std::istream & in, int * a, size_t s, size_t toread);
}
#endif
