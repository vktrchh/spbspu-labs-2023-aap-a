#include "inputShapes.hpp"
#include "base-types.hpp"
#include "rectangle.hpp"
#include "triangle.hpp"
#include "polygon.hpp"
#include <stdexcept>

void grechishnikov::inputShapes(std::istream &in)
{}
/*
grechishnikov::Shape* grechishnikov::inputRectangle(const double* values, size_t size)
{
  if (size != 2)
  {
    throw logic_error("Incorrect number of parameters");
  }
  return new grechishnikov::Rectangle()
}
grechishnikov::Shape* grechishnikov::inputTriangle(const double* values, size_t size)
{}
grechishnikov::Shape* grechishnikov::inputPolygon(const double* values, size_t size)
{}
*/
