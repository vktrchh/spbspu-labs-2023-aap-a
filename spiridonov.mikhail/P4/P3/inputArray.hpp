#ifndef INPUTARRAY_HPP
#define INPUTARRAY_HPP
#include <iostream>
#include <cstddef>
#include <iostream>

namespace spiridonov
{
  size_t * inputArray(const size_t * outdatedArray, size_t * currArray, const size_t str);
  void extencionArray(const size_t * outdatedArray, size_t * actualArray, size_t str);
}
#endif
