#ifndef DELETE_DECIMAL_DIGITS_HPP
#define DELETE_DECIMAL_DIGITS_HPP
#include <cstddef>

namespace chernov
{
  char* removeDecimalDigits(char* input, char* stringWithDecimalDigitsRemoved, size_t sizeOfInput);
}

#endif
