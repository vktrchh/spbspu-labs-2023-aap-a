#ifndef COUNT_NON_ZERO_DIAGONALS_HPP
#define COUNT_NON_ZERO_DIAGONALS_HPP

#include <cstddef>

namespace chernov
{
  size_t countNonZeroDiagonals(const int* matrix, size_t cols, size_t rows);
}
#endif
