#ifndef CHECKLOWERTRIANGULARMATRIX_H
#define CHECKLOWERTRIANGULARMATRIX_H
#include <cstddef>
namespace piyavkin
{
  bool checkLowerTriangularMatrix(size_t result, size_t rows, size_t cols, const int * matrix);
}
#endif
