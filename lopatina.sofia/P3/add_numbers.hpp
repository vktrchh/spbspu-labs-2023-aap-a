#ifndef ADD_NUMBERS_HPP
#define ADD_NUMBERS_HPP

#include <iostream>
namespace lopatina
{
  size_t countNumbers(const char * str);
  void addNumbers(char * first_str, const char * second_str, size_t size1, size_t size2, char * result);
}

#endif
